# A.X.L. portfolio — repository contract

Work only in this independent portfolio. Never modify SSSOM, Renata, Aplanta, their branches, processes or original media. Use a feature branch and PR without self-merge. The user authorized this public artist portfolio and requested axl.sssom.com; DNS changes are limited to axl, not the parent zone.

## Editorial truth
The supplied Links AXL.txt is authoritative for release years and selected links. Cover filenames may disagree. Everything Changed / Tudo Mudou is 2022, not 2021. Preserve original Portuguese release titles in both locales and distinguish Spotify service titles and collaborative credits. TRIZ is an additional supplied cover from 2016. Never invent a missing listening link, date precision, track count, award, audience statistic or transcript.
Use only the image from the public SSSOM profile: its biography explicitly contains fictitious data and must not be reused. Birth year 1990 and full name come from the user. Raw research belongs only in ignored artifacts. Contact is the public RUADOFLOW email, not a guessed personal address.

## Design: Apple-like + actual SSSOM typography
Read docs/design.md before UI changes. Black editorial surface, Inter body, Archivo display (width 110), 12/8/4 Müller grid and 8px spacing rhythm. No Hallmark, forced image cropping, decorative glow, noisy dashboard or visible debug controls. Use webfont CSS from Google Fonts; no font binaries distributed.
Use 44px minimum controls, visible focus, semantic anchors, one h1, reduced motion, native scrolling and full no-JavaScript release pages. Mobile navigation contains languages. Native dialog must close with Escape, contain keyboard focus, restore focus and handle history. External video only after explicit click. No audio download, auto-play or tracking integration.

## Build and hosting
Static English-first HTML and /pt-br/, per-release pages, equivalent Markdown/JSON and llms documents. No credentials, private originals or audit fixtures in output. Release files are generated from one source. Validate paths and escape editorial strings. No eval, innerHTML or inline executable JavaScript.
Preview is noindex and published to a separate branch; axl.sssom.com root build is distinct until DNS and HTTPS are verified. Do not redirect the usable preview into an unresolved domain. No production/indexation approval may be inferred from an automated test.
Run verify and design:apple; inspect screenshots and record true test counts and limitations. Browser audits are not Apple/WCAG certification, real-device touch tests or field performance measurements. Keep CSP and browser security policies intact.
