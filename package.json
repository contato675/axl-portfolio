{
  "name": "axl-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:domain": "node scripts/build.mjs --domain",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && npm run build && npm run build:domain",
    "preview": "node scripts/serve.mjs",
    "design:apple": "node scripts/audit.mjs",
    "publish:preview": "node scripts/publish.mjs --confirm-preview",
    "design:editorial": "node scripts/audit-editorial.mjs",
    "build:cloudflare": "node scripts/build-cloudflare.mjs",
    "publish:cloudflare": "node scripts/publish-cloudflare.mjs --confirm-domain",
    "verify:cloudflare": "node scripts/verify-cloudflare.mjs --release",
    "design:cloudflare": "node scripts/audit-cloudflare.mjs",
    "publish:cloudflare:release": "node scripts/publish-cloudflare.mjs --confirm-domain --release"
  }
}
